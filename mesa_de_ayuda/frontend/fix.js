const fs = require('fs');
const path = 'src/views/Tickets.jsx';
let content = fs.readFileSync(path, 'utf8');

const anchorText = \                    <select id="ticket-category-filter" value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)}>\n                      <option value="ALL">Todas las categorias</option>\n                      <option value="Hardware">Hardware</option>\n                      <option value="Software">Software</option>\n                      <option value="Redes">Redes</option>\n                    </select>\n                    <option value="Incidencia">Incidencia</option>\n                    <option value="Petición">Petición</option>\n                  </select>\n                </div>\n                \n                <div className="field full" style={{ gap: '0.2rem' }}>\n                  <label htmlFor="ticket-category" style={{ color: '#002E5D', fontWeight: 500 }}>Categoría <span style={{color: 'red'}}>*</span></label>\n                  <select id="ticket-category" value={form.category} onChange={(e) => setForm({...form, category: e.target.value})} required style={{ background: '#fff', border: '1px solid #ccc', borderRadius: '3px', padding: '0.4rem 0.6rem', width: '100%' }}>\n                    <option value="">-----</option>\n                    <option value="Hardware">Hardware</option>\n                    <option value="Software">Software</option>\n                    <option value="Redes">Redes</option>\n                  </select>\n                </div>\;

const newText = \                    <select id="ticket-category-filter" value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)}>\n                      <option value="ALL">Todas las categorias</option>\n                      <option value="Hardware">Hardware</option>\n                      <option value="Software">Software</option>\n                      <option value="Redes">Redes</option>\n                    </select>\n                  </div>\n                </div>\n              </div>\n              <div className="table-shell tickets-table-scroll" style={{ marginTop: '1rem' }}>\n                {filteredTickets.length === 0 ? (\n                  <div className="empty-state">No se encontraron tickets registrados que coincidan con la vista.</div>\n                ) : (\n                  <table>\n                    <thead>\n                      <tr>\n                        <th>ID</th>\n                        <th>Titulo</th>\n                        <th className="col-hide-mobile">Usuario</th>\n                        <th className="col-hide-mobile">Ubicación</th>\n                        <th>ANS</th>\n                        <th className="col-hide-mobile">Técnico Asignado</th>\n                        <th>Estado</th>\n                        <th className="col-hide-mobile">Fecha</th>\n                      </tr>\n                    </thead>\n                    <tbody>\n                      {filteredTickets.map((ticket) => (\n                        <tr key={ticket.id} onClick={() => handleSelectTicket(ticket)} style={{cursor: 'pointer', transition: 'background 0.2s'}} onMouseEnter={(e) => e.currentTarget.style.background = '#f0f7ff'} onMouseLeave={(e) => e.currentTarget.style.background = 'transparent'}>\n                          <td>{ticket.id}</td>\n                          <td>\n                            <strong>{ticket.title}</strong>\n                            <div className="muted-text">{stripHtml(ticket.description)}</div>\n                          </td>\n                          <td className="col-hide-mobile">\n                            <div style={{ display: 'flex', alignItems: 'center', gap: '0.55rem' }}>\n                              <AvatarThumb\n                                name={ticket.createdBy?.name || 'Administrador'}\n                                avatarUrl={ticket.createdBy?.avatarUrl}\n                                borderColor="#d6dde6"\n                              />\n                              <span>{ticket.createdBy?.name || 'Administrador'}</span>\n                            </div>\n                          </td>\n                          <td className="col-hide-mobile">{ticket.location?.name || 'No aplica'}</td>\n                          <td>\n                            <SlaProgressBar createdAt={ticket.createdAt} sla={ticket.sla} resolvedAt={ticket.resolvedAt || ticket.closedAt} status={ticket.status} />\n                          </td>\n                          <td className="col-hide-mobile">\n                            {(() => {\n                              const rIds = getResponsibleUserIds(ticket);\n                              const mainTechId = rIds[0];\n                              const mainTech = mainTechId ? users.find(u => String(u.id) === String(mainTechId)) : null;\n                              return (\n                                <span style={{ color: mainTech ? '#1a437e' : '#999', fontWeight: 600, fontSize: '0.85rem', display: 'flex', alignItems: 'center', gap: '0.4rem' }}>\n                                  {mainTech ? mainTech.name : '-'}\n                                </span>\n                              );\n                            })()}\n                          </td>\n                          <td>\n                            <span className={\adge \\}>{getStatusLabel(ticket.status)}</span>\n                          </td>\n                          <td className="col-hide-mobile">{new Date(ticket.createdAt).toLocaleDateString()}</td>\n                        </tr>\n                      ))}\n                    </tbody>\n                  </table>\n                )}\n              </div>\n            </article>\n\n            <article className="card">\n              <h3 style={{ marginBottom: '1.2rem' }}>Crear Nuevo Ticket</h3>\n              <form style={{ display: 'flex', flexDirection: 'column', gap: '1.2rem' }} onSubmit={handleSubmit}>\n                <div className="field full" style={{ gap: '0.2rem' }}>\n                  <label htmlFor="ticket-type" style={{ color: '#002E5D', fontWeight: 500 }}>Tipo</label>\n                  <select id="ticket-type" value={form.ticketType} onChange={(e) => setForm({...form, ticketType: e.target.value})} style={{ background: '#fff', border: '1px solid #ccc', borderRadius: '3px', padding: '0.4rem 0.6rem' }}>\n                    <option value="Incidencia">Incidencia</option>\n                    <option value="Petición">Petición</option>\n                  </select>\n                </div>\n                \n                <div className="field full" style={{ gap: '0.2rem' }}>\n                  <label htmlFor="ticket-category" style={{ color: '#002E5D', fontWeight: 500 }}>Categoría <span style={{color: 'red'}}>*</span></label>\n                  <select id="ticket-category" value={form.category} onChange={(e) => setForm({...form, category: e.target.value})} required style={{ background: '#fff', border: '1px solid #ccc', borderRadius: '3px', padding: '0.4rem 0.6rem', width: '100%' }}>\n                    <option value="">-----</option>\n                    <option value="Hardware">Hardware</option>\n                    <option value="Software">Software</option>\n                    <option value="Redes">Redes</option>\n                  </select>\n                </div>\;

if(content.includes(anchorText)) { content = content.replace(anchorText, newText); fs.writeFileSync(path, content); console.log('Fixed'); } else { console.log('Not found'); }
